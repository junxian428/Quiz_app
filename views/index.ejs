<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <style>
        body {
            background-color: #222;
            color: #fff;
            font-family: Arial, sans-serif;
        }
        form {
            background-color: #333;
            padding: 20px;
            border-radius: 10px;
            max-width: 400px;
            margin: 0 auto;
        }
        input, button {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            color: #222;
        }
        input[type="submit"] {
            background-color: #00f;
            color: #fff;
            cursor: pointer;
        }
        input[type="submit"]:hover {
            background-color: #003;
        }
        input::placeholder {
            color: #666;
        }
        h1 {
            text-align: center;
        }
    </style>
</head>
<body>

    <h1 style="margin-top:150px;"><%= message %></h1>
    <br>
    <br>
    <form action="/login" method="post">
        <input type="text" name="username" placeholder="Username" style="width: 100%; box-sizing: border-box; padding: 10px;">
        <input type="password" name="password" placeholder="Password" style="width: 100%; box-sizing: border-box; padding: 10px;">
        <button type="submit" style="width: 100%; box-sizing: border-box; padding: 10px; background-color: #00ff00; color: #fff;">Login</button>
    </form>
    <p style="text-align: center;"><%= errormessage %></p>
</body>
</html>
