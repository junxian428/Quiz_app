<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <style>
        body {
            background-color: #222;
            color: #fff;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }
        h1 {
            text-align: center;
        }
        button {
            margin: 10px;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 150px; /* Adjust the width as needed */
        }
        .start-button {
            background-color: #00ff00;
            color: #fff;
        }
        .change-username-button {
            background-color: #ff4500;
            color: #fff;
        }
        .dashboard-button {
            background-color: #007bff; /* Blue color for dashboard button */
            color: #fff;
        }
    </style>
</head>
<body>
    <h1><%= message %></h1>
    <button class="start-button" style="text-decoration: none;">
        <a href="/quiz?username=<%= username %>" style="text-decoration: none; color:white;"> <%= username %> Start the Quiz</a>
    </button>
    <button class="change-username-button">
        <a href="/change-username?username=<%= username %>" style="text-decoration: none; color:white;">
          Change Username
        </a>
      </button>
      
</body>
</html>
